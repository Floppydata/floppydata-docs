---
title: 'Account Usage'
openapi: '/api-reference/openapi.json GET /v2/account/usage'
---

<Note>
  Usage responses include `yesterday`, `last7Days`, `last30Days`, `lastUpdatedAt`, and `requestedRange` when `from` or `to` is supplied.
</Note>
