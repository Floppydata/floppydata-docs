---
title: 'Delete Session History'
openapi: '/api-reference/openapi.json DELETE /v2/browser/sessions/{sessionId}'
---

<Note>
  This deletes an ended session from history only. It does not delete reusable persistent browser state; use Delete Saved Settings when you want to remove saved state before its retention window expires.
</Note>
