---
title: 'Get a Session'
openapi: '/api-reference/openapi.json GET /v2/browser/sessions/{sessionId}'
---

<Note>
  Use this operation to check current state or obtain a freshly signed `connectUrl` while the session is active. Ended sessions return `connectUrl: null`.
</Note>
