---
title: 'List Sessions'
openapi: '/api-reference/openapi.json GET /v2/browser/sessions'
---

<Note>
  Session history is newest first. Filter by `status`, use the opaque `nextCursor` unchanged, and reuse an item’s `settings.id` only when `settings.persistence` is `persistent` and the retention window has not expired.
</Note>
