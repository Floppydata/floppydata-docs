---
title: 'Stop a Session'
openapi: '/api-reference/openapi.json POST /v2/browser/sessions/{sessionId}/stop'
---

<Note>
  Stopping is idempotent. Burner sessions begin browser-state cleanup after stop; persistent sessions receive a `retentionExpiresAt` value based on their `persist.ttlSeconds`.
</Note>
